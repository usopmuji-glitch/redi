# render.yaml - Render Blueprint (ejemplo simple)
services:
  - type: web
    name: mi-servicio
    env: docker
    repo: https://github.com/fullmoney979-rgb/dkddd.git
    branch: main
    dockerfilePath: dockerfile
    envVars:
      - key: APP_ENV
        value: production
